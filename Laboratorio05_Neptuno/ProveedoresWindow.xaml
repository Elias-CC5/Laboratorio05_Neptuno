<Window x:Class="Laboratorio05_Neptuno.ProveedoresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Proveedores y Búsqueda" Height="450" Width="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filtros de búsqueda -->
        <GroupBox Header="Búsqueda / Filtros" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Contacto:"/>
                <TextBox x:Name="txtBuscarContacto" Width="120" Margin="5,0"/>
                <Label Content="Ciudad:"/>
                <TextBox x:Name="txtBuscarCiudad" Width="120" Margin="5,0"/>
                <Button x:Name="btnBuscar" Content="Buscar" Width="80" Margin="10,0" Click="btnBuscar_Click"/>
            </StackPanel>
        </GroupBox>

        <!-- Botones Mantenimiento -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnEliminar" Content="Eliminar (Baja Lógica)" Width="140" Click="btnEliminar_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgProveedores" Grid.Row="2" AutoGenerateColumns="True"/>
    </Grid>
</Window>